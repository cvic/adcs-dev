---
import { useTranslations, type Lang } from "@/i18n";
import { getRelativeLocaleUrl } from "astro:i18n";

const t = useTranslations(Astro.currentLocale as Lang);
const locale = Astro.currentLocale as Lang;
---

<div class="alert_important">
  <p>
    <span class="red">
      {
        t({
          en: "IMPORTANT! This is not an emergency request.If the situation is critical, call 911!",
          ro: "IMPORTANT! Acest sistem nu este unul de urgență. Dacă situația este critică, apelează 112!",
        })
      }
    </span>
    <button id="more_info_button" class="btn btn-primary test_on_button"> ▼ {t({
      en: " I agree that I am requesting help from the A.D.C.S. community and that I am not in a critical situation requiring a call to the emergency number 112. I understand that my request may be rejected if it does not fall within the scope of A.D.C.S. activities. I agree that volunteers are not liable for any damages, and that my data (name, phone number, location) will be made available to the community.",
      ro: "Sunt de acord că solicit ajutor din partea comunității A.D.C.S. și nu mă aflu într-o situație critică ce impune apelarea numărului de urgență 112. Sunt de acord ca solicitarea mea poate fi respinsă dacă nu intră în aria de activitate a A.D.C.S. Sunt de acord că voluntarii sunt exonerati de răspunderea pentru eventuale daune, iar datele mele (nume, telefon, localizare) vor fi puse la dispoziție comunității.",
    })}</button>
  </p>
</div>

<style>
  .alert_important {
    border-radius: 8px;
    background-color: color-mix(in srgb, var(--color-caution) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-caution) 30%, transparent);
    border-top: 3px solid var(--color-caution);
    padding: var(--sp-s) var(--sp-m);
    text-align: center;
    margin-block-start: var(--sp-m);
  }

  .red {
    display: block;
    color: var(--color-accent);
    font-size: 1rem;
    font-weight: 700;
    padding-block: var(--sp-s);
    line-height: 1.5;
  }

  #more_info_button {
    display: block;
    width: 100%;
    margin-block-start: var(--sp-s);
    padding: var(--sp-s) var(--sp-m);
    text-align: left;
    font-size: 0.8rem;
    line-height: 1.6;
    background-color: color-mix(in srgb, var(--color-main) 6%, transparent);
    border-radius: 6px;
    border: 1px solid color-mix(in srgb, var(--color-main) 15%, transparent);
    border-left: 3px solid var(--color-accent);
    color: var(--color-main);
    cursor: pointer;
    box-shadow: none;
    transform: none;

    &:hover {
      background-color: color-mix(in srgb, var(--color-main) 10%, transparent);
      border-left-color: var(--color-theme);
      transform: none;
      box-shadow: none;
    }
  }
</style>

<script>
  const btn = document.getElementById("more_info_button");
  const comp = document.getElementById("hiddenComponent");

  if (btn && comp) {
    btn.addEventListener("click", () => {
      if (comp.classList.contains("show")) {
        // fade + slide OUT
        comp.classList.remove("show");
        setTimeout(() => comp.classList.add("hidden"), 500); // wait for transition
      } else {
        // fade + slide IN
        comp.classList.remove("hidden"); // show it instantly, but still opacity 0
        void comp.offsetWidth; // 🔑 force reflow so CSS transition can start
        comp.classList.add("show"); // now animate opacity 0 → 1 & height 0 → max
      }
    });
  }
</script>
